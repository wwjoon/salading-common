export enum PlanArchiveState {
  Active = 'active',
  Archived = 'archived'
}

export enum PlanType {
  Single = 'single',
  Weekly = 'weekly',
  WeeklyTwo = 'weeklyTwo'
}

export enum PlanDetailStatus {
  Temp = 'temp',
  Confirmed = 'confirmed'
}
