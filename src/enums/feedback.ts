export enum FeedbackType {
  Review = 'review',
  Complaint = 'complaint'
}

export enum FeedbackRoute {
  Homepage = 'homepage',
  Kakao = 'kakao',
  Phone = 'phone'
}

export enum FeedbackSolutionType {
  Refund = 'refund',
  Return = 'return',
  Exchange = 'exchange'
}

export enum FeedbackRewardMethod {
  Refund = 'refund',
  Mileage = 'mileage'
}

export enum FeedbackCancelState {
  WaitingEum = 'waitingEUM',
  Rejected = 'rejected',
  Pending = 'pending',
  Applied = 'applied'
}
